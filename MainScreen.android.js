'use strict';

var React = require('react-native');

var {
  View,
  Text,
  AsyncStorage,
  ListView,
  Image,
} = React;

var Nav = require('./Nav');
var ViewPager = require('react-native-viewpager');
